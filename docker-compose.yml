version: '3.8'

services:
  catalog-dashboard:
    build: .
    container_name: catalog-dashboard
    restart: unless-stopped
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
